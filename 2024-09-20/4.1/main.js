const par= (num)=> {
    if (num%2==0) return true;
    else return false;
}

const par2= (num)=> { return (num%2==0) }

const par3= (num)=> (num%2==0)

const ullman = (num)=> {
    if (num<1) {
        document.write("el numero es menor que 1");
    }else{













    }
}